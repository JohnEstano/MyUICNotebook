import { SVGAttributes } from 'react';
import { StickyNote, Bird } from 'lucide-react';


export default function AppLogoIcon(props: SVGAttributes<SVGElement>) {
    return (
        <StickyNote className="size-10 text-pink-500 dark:text-pink-500" />
    );
}
